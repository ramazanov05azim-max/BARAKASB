# Docker

Future development and CI containers. Images must use minimal pinned bases, non-root
users, multi-stage builds, health checks, read-only filesystems where possible, and no
embedded secrets.
