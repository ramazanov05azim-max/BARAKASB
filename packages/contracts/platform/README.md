# Platform contracts

Owns transport-neutral schema primitives, contract metadata, and compatibility test
utilities. Domain entities and persistence models are not contracts.

The package defines the neutral `SolutionRuntimeManifest` contract used across the
browser composition boundary. It contains no Coffee fields, framework types, transport
types, or runtime implementation.
