# Object storage

Owns project-scoped object references, signed-access ports, checksum validation,
quarantine state, and provider adapters. Domain-facing ports and business attachment
rules remain in the owning module.
